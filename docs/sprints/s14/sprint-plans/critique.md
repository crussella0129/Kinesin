# Plan Critique — Sprint 14

## Concerns

(none — plans are clean per the failure-mode screen.)

The independent review covered the research report, frozen-workload draft,
build/test plans, sprint metadata and linked INT-0032/0024/0031/0030 chapters.
Each T-115–T-117 EARS clause maps to named verification. Recovery is limited
to new-version freeform work with compiled effectful grants; two repair
nudges and a separate once-per-run completion review consume existing budgets.
Bounded receipt summaries preserve actual effects across compaction without
retaining arbitrary payloads or upgrading unchecked acceptance. Rejected
prose cannot become a terminal candidate, and legacy replay remains covered.

The plan preserves exact-edit refusal, private paths, grants, checked/no-tool
behavior and existing operator profiles. It acknowledges that model review
cannot establish arbitrary app correctness. The live card therefore requires
independent browser outcomes from exactly two natural task messages, with no
operator corrections, code, tool-forcing instructions or context resets in an
accepted attempt. Failed attempts and internal recovery cost remain visible;
no unmeasured human-authoring speedup or broad model reliability is claimed.

The implementation owner agreed the concrete scope and ceilings before this
review. Profile/source identity must be recorded before each scored dispatch.
Official unit/integration checks remain after the successful live confidence
gate, consistent with the user's instruction.

## Confidence

clean

## Review of the attempt-1 implementation refinement

The [recorded refinement](../sprint-research/implementation-adjustment.md) and
explicit INT-0032 consequence were reviewed after the failed first live attempt.
At most six typed milestones replace manual task decomposition without new
effects, budgets or operator instructions. Stage-specific successful-operation
witnesses reject unsupported step completion, while the existing two repair
nudges and one final review remain bounded. Core-4 behavior must remain available
for the already captured attempt; core 5 owns the new execution semantics.

This is an accepted implementation refinement within T-115, not an acceptance
of the failed app or a weakening of the frozen live rubric. The existing named
budget/authority/replay checks must cover the added states after live confidence.
No locked plan or independent outcome criterion was silently changed. Review
confidence for this refinement: clean.

## Review of the attempt-2 implementation refinement

The additional [core-6 refinement](../sprint-research/implementation-adjustment.md)
is accepted within T-115. It addresses the observed empty-response exhaustion
and incomplete planning without interpreting prose as an effect. Advancing on a
stage's real successful operation is explicitly weaker than verification; it
must carry that completion basis, admit no candidate, and leave final review and
the independent browser gate intact. No-witness empties still use the shared
two-repair allowance. Core-4/core-5 compatibility and all ceilings remain binding.
The next attempt is fresh and separately frozen; failed artifacts remain visible.
No locked plan or outcome criterion changes. Refinement confidence: clean.

## Review of the attempt-3 and diagnostic refinement

Independent design review by `live_sprint_setup` accepts the
[core-7 refinement](../sprint-research/implementation-adjustment.md) within
T-115, including the explicitly documented `src/preview.rs` scope extension.
The evidence shows that stage classification and same-stage witness rules can
false-stop real work, while successful operations did not establish application
correctness. Removing these new-run gates is smaller and clearer than adding
special-case stage aliases or receipt-reuse exceptions.

Acceptance of the design requires exact core-4/core-5/core-6 replay, short
generic instructions, unchanged two-repair/one-review ceilings and authority.
The bounded tools-5 missing-file hint and prerequisite-result guidance directly
address the diagnostic's guessed wrong-root batch; neither performs filesystem
enumeration nor grants it. Adapter 4 and historical core/tools replay remain
unchanged. Review may request a fresh same-path preview observation after repair
without treating it as semantic verification.
Preview startup warnings must accompany a successful real URL, refresh from
current bounded index bytes on repeated calls, fit existing output limits and
preserve worker/deadline/server ownership. Index-only warnings cannot claim
browser verification, and their absence cannot upgrade unchecked acceptance.
CSP and grants remain unchanged.

The read-only diagnostic failures are not successful app repairs. The next
scored attempt remains fresh and independently observed under the same frozen
two-prompt rubric. No task completion, official test result, locked-plan change
or successful live gate is implied. Refinement confidence: clean.

## Review of the six-failure structured-action refinement

Independent review by `live_sprint_setup` accepts one bounded core-8/adapter-5
[structured single-action experiment](../sprint-research/implementation-adjustment.md#attempt-7-bounded-structured-single-action-protocol)
within T-115, with tools 5 unchanged. It targets the recurring gap between
proposed code/repairs and actual actions. The first-action probe is insufficient
evidence of usefulness; complete app behavior must still be observed live.

The declared schema permits one exact granted compiled-tool action or an answer,
only for freeform all-compiled runs with an effectful grant. Normal authorization,
typed arguments, budgets, journaling and dispatch remain binding after parsing.
Malformed/partial/unknown-field responses and arbitrary prose produce no effects;
invalid-protocol recovery shares two nudges and one separate final review.
Streaming withholds raw action JSON/provisional arguments. Checked, read-only
and all MCP-containing runs remain native; previous replay stays exact. No rigid
plan stages or automatic acceptance are introduced.

Pre-dispatch implementation inspection identified and resolved an encoded-history
budget gap: adapter 5 checks its transformed message array, including protocol
instructions and schemas, against the frozen cap before dispatch. Runner and
replay pass that same cap; an oversized request truthfully stops after ordinary
logical compaction, with no extra allowance or recovery loop. The native branch
does not serialize the new option, preserving prior request bytes. This was
read-only review of the implemented fix, not an official test result.

Attempt 7 must restore the original baseline model/profile/ceilings, separately
freeze the new source/binary and retain all failures. The independent browser
criteria and zero-correction rule remain unchanged. No source implementation,
official test result or success is asserted by this design review, and locked
plans remain untouched. Refinement confidence: clean.

## Review of the rejected structured experiment and rollback

Independent review by `live_sprint_setup` accepts the
[explicit rollback](../sprint-research/implementation-adjustment.md#rejected-structured-experiment-and-default-rollback)
after attempts 7 and 8 each produced two schema-valid answers but zero tool
effects. Attempt 8 regressed from native attempt 5's observed file/product
creation under the same model profile, although neither passed the live gate.

Restore exact core-7/adapter-4/tools-5 new-run defaults and disable structured
actions in runner options. Retain core-8/adapter-5 as an explicitly admitted
experimental capture version, selecting its structured reconstruction only for
core 8. Keeping its decoder and encoded-history cap preserves evidence without
making the rejected mode the default. No core-9 semantics are introduced.

This accepts the rollback design by code inspection, not completed source
verification or product success. The usability gate stays unmet and T-115–T-117
remain queued/incomplete; no official tests or locked-plan changes are implied.
The next design must use actual independent observations for bounded repair and
completion, rather than continue blind prompt variants. Refinement confidence:
clean.
